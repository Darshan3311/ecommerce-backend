# Backend production environment variables for Render (example)
NODE_ENV=production
# Render will set PORT automatically; keep a fallback
PORT=5000

# Database (MongoDB Atlas)
MONGODB_URI=mongodb+srv://Darshan:Darshan2003@cluster0.kpm1vpd.mongodb.net/ecommerce

# JWT secrets (use strong, random values in Render dashboard - do NOT commit real secrets)
JWT_SECRET=replace_with_strong_jwt_secret
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=replace_with_strong_refresh_secret
JWT_REFRESH_EXPIRE=30d

# Email (transactional SMTP provider credentials)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=work.darshhhuuuuu@gmail.com
EMAIL_PASSWORD=replace_with_real_email_password_or_sendgrid_api_key
EMAIL_FROM=noreply@ecommerce.com

# Cloudinary
CLOUDINARY_CLOUD_NAME=dr62ilpwv
CLOUDINARY_API_KEY=224349418764251
CLOUDINARY_API_SECRET=UbN9Am1vVHoPGdjhrjFp71FpvsM

# Stripe (if used)
STRIPE_SECRET_KEY=replace_with_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=replace_with_stripe_publishable_key

# Production frontend URLs allowed by CORS (comma separated)
# Example: https://ecommerce-frontend-abc.onrender.com
FRONTEND_URLS=https://ecommerce-frontend-2-a2w2.onrender.com

# Public backend base URL (useful for emails or external links)
BACKEND_URL=https://ecommerce-backend-k1lz.onrender.com

# Recommended: set CLOUDINARY_URL in Render environment variables in the format:
# cloudinary://<API_KEY>:<API_SECRET>@<CLOUD_NAME>
# Example (DO NOT commit real secrets to git):
# CLOUDINARY_URL=cloudinary://224349418764251:UbN9Am1vVHoPGdjhrjFp71FpvsM@dr62ilpwv

# Security
SESSION_SECRET=replace_with_session_secret

# Optional: allow all origins (DO NOT enable in production)
# ALLOW_ALL_ORIGINS=false

# Notes:
# - On Render: add these variables in the Service Environment settings (do not commit secrets to git).
# - Ensure NODE_ENV=production so cookies are set with secure=true and sameSite=None for cross-site cookies.