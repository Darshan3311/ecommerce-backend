# Production Environment Variables Template
# Copy these to Render Dashboard → Environment Variables

# Server Configuration
NODE_ENV=production
PORT=10000

# Database (MongoDB Atlas - REQUIRED)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ecommerce?retryWrites=true&w=majority

# JWT Configuration (REQUIRED - Generate strong secrets)
JWT_SECRET=generate_a_strong_random_secret_key_here_min_32_chars
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=generate_another_strong_random_secret_key_here
JWT_REFRESH_EXPIRE=30d

# Email Configuration (Optional - for email verification)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password
EMAIL_FROM=noreply@ecommerce.com
CLIENT_URL=https://your-frontend-url.onrender.com

# Cloudinary Configuration (REQUIRED - for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Frontend URL (REQUIRED - for CORS)
FRONTEND_URL=https://your-frontend-url.onrender.com

# Session Secret (REQUIRED)
SESSION_SECRET=generate_a_strong_session_secret_key_here

# Notes:
# 1. Replace all placeholder values with your actual credentials
# 2. For MongoDB Atlas: Create free cluster at mongodb.com/cloud/atlas
# 3. For Cloudinary: Sign up at cloudinary.com for free tier
# 4. For Gmail: Use App Password, not regular password (myaccount.google.com/apppasswords)
# 5. FRONTEND_URL should be your deployed React app URL from Render Static Site
